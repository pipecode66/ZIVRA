{
  "name": "zivra-landing",
  "version": "1.0.0",
  "private": true,
  "description": "Landing de ZIVRA Studio lista para Vercel",
  "scripts": {
    "build": "echo Static site: no build step required",
    "dev": "npx serve . -l 3000",
    "start": "npx serve . -l 3000"
  },
  "engines": {
    "node": ">=18"
  }
}
